server:
  tomcat:
    uri-encoding: UTF-8
  port: 8003
spring:
  profiles:
    active: dev
  application:
    name: node12-server
  http:
    encoding:
      charset: UTF-8
      force: true
      enabled: true